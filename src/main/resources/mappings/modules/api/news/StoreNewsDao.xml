<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uwca.operation.modules.api.news.dao.StoreNewsDao">
	<insert id="insert">
		INSERT INTO storeinfo(
		id,
		userid,
		newsid
		) VALUES (
		#{id},
		#{userid},
		#{newsid}
		)
	</insert>

	<select id="getStoreNews" resultType="com.uwca.operation.modules.api.news.entity.po.StoreNews">
		select * from storeinfo where userid=#{userid} and newsid=#{userid} and isdel=0
	</select>
	
	<update id="delStoreNews">
		update storeinfo set isdel = 1 where id = #{id}
	</update>
</mapper>